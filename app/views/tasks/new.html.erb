<h1>タスク作成ページ</h1>

<%= form_with(model: @task ) do |f| %>
  <%= f.label :content, "タスク" %>
  <%= f.text_field :content %>
  
  <%= f.label :status, "ステータス" %>
  <%= f.text_field :status %>
  
  <%= f.submit "追加" %>
<% end %>

<%= link_to "一覧に戻る", tasks_path %>